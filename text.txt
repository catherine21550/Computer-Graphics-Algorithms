поки є стек В потрібно йти по лупу:
1). find closest (де буде створено inf array, .ind_clos)
2). fill_one (заповнить .closest, .nb_ontop)
3). fill_two (заповнить .nb, .clos_ontop, .nb_index, .res_mvs)
4). знайти мінімальну кількіть операцій find_min_mvs, ф-ція віддає 
індекс того числа, який рухати в стеку B нагору та той самий індекс для потрібної ноди в структурі інформації
5). Далі передаємо ind_clos з ноди з поперелньої ф-ції у clos_ontop і рухаємо найближче число нагорую
6). Передаємо ідекс числа з ноди в nb_ontop і рухаємо число нагору.
7). push_a перекладає з В в А
8). free(масив структур)

9). Далі ми знаходимо найменше в А та робимо ротацію доки воно не стане першим числом.
10).Перевіряємо чи стек А сортований.
11).Завершуємо програму.
